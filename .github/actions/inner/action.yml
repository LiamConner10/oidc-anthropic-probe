name: 'inner-action'
description: 'Inner composite action — content security test'
runs:
  using: 'composite'
  steps:
    - name: Mark which version is running
      shell: bash
      run: |
        echo "Running INNER-ATTACKER-EVIL version"
        echo "INNER_VERSION=attacker-EVIL" >> "$GITHUB_ENV"
